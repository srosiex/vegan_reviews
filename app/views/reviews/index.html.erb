<% if @meal %>
    <h1>AView all the reviews for <%= @meal.name %></h1>
    <% else %>
    <h1>View all reviews </h1>
<% end %>

<% @reviews.each do |r| %>
    <h2><%= r.meal.name %></h2> 
    <h3><%= r.title %></h3>
    <%= r.stars %><br>
    <%= r.content %><br>
    <% end %>